import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { MemoryRouter, Route, Routes } from "react-router-dom";
import { beforeEach, describe, expect, it, vi } from "vitest";
import * as workspace from "../../auth/WorkspaceContext";
import * as experiencesApi from "../../api/experiences"; import * as pagesApi from "../../api/pages"; import * as segmentsApi from "../../api/segments"; import * as eventsApi from "../../api/events";
import { Sidebar } from "../../components/Sidebar";
import { AppShell } from "../../components/AppShell";
import { CreateExperienceModal } from "./CreateExperienceModal"; import { ExperienceListPage } from "./ExperienceListPage"; import { ExperienceEditorPage } from "./ExperienceEditorPage";
import type { Experience } from "../../types/experiences";

vi.mock("../../auth/WorkspaceContext"); vi.mock("../../api/experiences"); vi.mock("../../api/pages"); vi.mock("../../api/segments"); vi.mock("../../api/events");
const builderHarness=vi.hoisted(()=>({flush:vi.fn(),render:vi.fn()}));
vi.mock("../../components/experiences/GrapesWidgetBuilder",async()=>{const React=await import("react");return{GrapesWidgetBuilder:React.forwardRef(function Builder({experienceKey,widgetType,value,content,onChange}:{experienceKey:string;widgetType:string;value?:{html:string;projectData?:unknown};content:{heading:string;body:string};onChange:(value:unknown)=>void},ref){builderHarness.render(experienceKey,widgetType,value);React.useImperativeHandle(ref,()=>({flush:()=>builderHarness.flush(experienceKey)}),[experienceKey]);const heading=content.heading==="Hello"?"Latest heading":`${content.heading} edited`;return <div><output aria-label="Builder experience key">{experienceKey}</output><output aria-label="Builder widget type">{widgetType}</output><output aria-label="Loaded builder HTML">{value?.html??"No builder"}</output><output aria-label="Loaded builder project">{JSON.stringify(value?.projectData??null)}</output><button onClick={()=>onChange({builder:{version:1,projectData:{pages:[{id:experienceKey,freePosition:{left:140,top:92,width:280}}]},html:`<section class="movecues-widget"><h2 data-movecues-content="heading">${heading}</h2></section>`,css:".movecues-widget{}"},content:{...content,heading}})}>Change builder</button></div>;})}});
const popupHarness=vi.hoisted(()=>({location:{href:"",replace:vi.fn()},close:vi.fn(),focus:vi.fn()}));
const mockedWorkspace=vi.mocked(workspace); const mockedExperiences=vi.mocked(experiencesApi); const mockedPages=vi.mocked(pagesApi); const mockedSegments=vi.mocked(segmentsApi); const mockedEvents=vi.mocked(eventsApi);
const page={id:"pg_1",siteId:"site_1",name:"Home",description:null,area:null,pageType:null,rules:[{id:"r1",kind:"include" as const,operator:"equals" as const,value:"/home"}],heatmapEnabled:false,views:0,uniqueVisitors:0,uniqueSessions:0,lastSeenAt:null,createdAt:"2026-01-01",updatedAt:"2026-01-01"};
const definition={content:{heading:"Hello",body:"World"},design:{width:"md" as const,theme:{background:"#ffffff",foreground:"#111827",primary:"#2563eb",borderRadius:"md" as const}},behavior:{dismissible:true,toastPosition:"bottom-right" as const},targeting:{pageRules:[],audience:{type:"all" as const},trigger:{type:"page_load" as const},frequency:{mode:"once" as const},priority:0}};
const experience:Experience={id:"exp_1",siteId:"site_1",kind:"widget",widgetType:"anchored_card",name:"Welcome",status:"draft",buildPageId:"pg_1",buildUrl:"https://app.example.com/home",publishedVersionId:null,createdBy:"u1",createdAt:"2026-01-01",updatedAt:"2026-01-01",draftVersion:{id:"v1",experienceId:"exp_1",versionNumber:1,state:"draft",definition,createdBy:"u1",createdAt:"2026-01-01",publishedAt:null},publishedVersion:null};
const guideExperience=():Experience=>({...experience,kind:"guide",widgetType:null,name:"Guide",draftVersion:{...experience.draftVersion!,definition:{steps:[{id:"step_1",content:{heading:"Welcome",body:"First"},target:{primarySelector:"#first",fallbackSelectors:[],reliability:"reliable",targetContext:{pagePath:"/onboarding?source=guide#intro"}},behavior:{placement:"auto",alignment:"center",offset:8,dismissible:true}},{id:"step_2",content:{heading:"Create project",body:"Second"},target:{primarySelector:"#create",fallbackSelectors:[],reliability:"reliable",targetContext:{pagePath:"/projects/new?template=blank#details"}},behavior:{placement:"auto",alignment:"center",offset:8,dismissible:true}}],design:definition.design,targeting:definition.targeting}}});
const surveyExperience=():Experience=>({...experience,widgetType:"survey",name:"Survey",draftVersion:{...experience.draftVersion!,definition:{...definition,behavior:{dismissible:true,modalLayout:"center",backdrop:true},survey:{showProgress:true,allowBack:true,submitLabel:"Submit",steps:[{id:"survey_1",content:{heading:"Intro",body:"First"},questions:[{id:"q1",type:"rating",label:"Rate",min:1,max:5}],builder:{version:1,projectData:{},html:'<section class="movecues-widget">Intro saved</section>',css:".movecues-widget{}"}},{id:"survey_2",content:{heading:"Follow-up",body:"Second"},questions:[]}]}}}});
function ready(){mockedWorkspace.useWorkspace.mockReturnValue({orgs:[],currentOrg:{orgId:"org_1",name:"Acme",role:"OWNER"},setCurrentOrgId:vi.fn(),sites:[],currentSite:{id:"site_1",siteId:"public_1",name:"App",domain:"app.example.com"},setCurrentSiteId:vi.fn(),loading:false,error:null,refreshSites:vi.fn()}); mockedPages.listPages.mockResolvedValue({pages:[page]});}

describe("Experiences dashboard",()=>{
  beforeEach(()=>{vi.clearAllMocks();ready();mockedSegments.listSegments.mockResolvedValue({segments:[],total:0,limit:100,offset:0});mockedEvents.listEvents.mockResolvedValue({events:[],total:0,limit:100,offset:0});vi.spyOn(window,"open").mockReturnValue(popupHarness as unknown as Window);});
  it("renders the experience collection routes without generic widgets or visual builder",()=>{render(<MemoryRouter><Sidebar onOpenSettings={vi.fn()}/></MemoryRouter>);expect(screen.getByRole("link",{name:"Guides"})).toHaveAttribute("href","/experiences/guides");expect(screen.getByRole("link",{name:"Modals"})).toHaveAttribute("href","/experiences/modals");expect(screen.getByRole("link",{name:"Surveys"})).toHaveAttribute("href","/experiences/surveys");expect(screen.getByRole("link",{name:"Tooltips"})).toHaveAttribute("href","/experiences/tooltips");expect(screen.getByRole("link",{name:"Banners"})).toHaveAttribute("href","/experiences/banners");expect(screen.getByRole("link",{name:"Checklists"})).toHaveAttribute("href","/experiences/checklists");expect(screen.queryByRole("link",{name:"Widgets"})).not.toBeInTheDocument();expect(screen.queryByRole("link",{name:"Visual Builder"})).not.toBeInTheDocument();});
  it("filters each supported collection and isolates unsupported checklists",async()=>{mockedExperiences.listExperiences.mockResolvedValue({experiences:[]});const {rerender}=render(<MemoryRouter><ExperienceListPage kind="guide"/></MemoryRouter>);expect(await screen.findByText("Create your first guide.")).toBeInTheDocument();expect(mockedExperiences.listExperiences).toHaveBeenLastCalledWith("org_1","site_1","guide",undefined);rerender(<MemoryRouter><ExperienceListPage kind="widget" widgetType="modal" singularLabel="modal" pluralLabel="Modals"/></MemoryRouter>);expect(await screen.findByText("Create your first modal.")).toBeInTheDocument();expect(mockedExperiences.listExperiences).toHaveBeenLastCalledWith("org_1","site_1","widget","modal");rerender(<MemoryRouter><ExperienceListPage kind="widget" singularLabel="checklist" pluralLabel="Checklists" unsupported/></MemoryRouter>);expect(await screen.findByText("Checklists are not available yet.")).toBeInTheDocument();});
  it("deletes a listed experience and does not expose a preview action",async()=>{mockedExperiences.listExperiences.mockResolvedValue({experiences:[experience]});mockedExperiences.deleteExperience.mockResolvedValue(undefined);vi.spyOn(window,"confirm").mockReturnValue(true);render(<MemoryRouter><ExperienceListPage kind="widget" widgetType="anchored_card" singularLabel="tooltip" pluralLabel="Tooltips"/></MemoryRouter>);expect(await screen.findByText("Welcome")).toBeInTheDocument();expect(screen.queryByRole("button",{name:"Preview"})).not.toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Delete Welcome"}));await waitFor(()=>expect(mockedExperiences.deleteExperience).toHaveBeenCalledWith("org_1","site_1","exp_1"));expect(window.confirm).toHaveBeenCalledWith('Delete "Welcome"? This cannot be undone.');});
  it("creates a modal without exposing a generic widget selector or changing build-page targeting",async()=>{mockedExperiences.createExperience.mockResolvedValue(experience);render(<MemoryRouter><CreateExperienceModal kind="widget" widgetType="modal" singularLabel="modal" open onOpenChange={vi.fn()}/></MemoryRouter>);await screen.findByRole("option",{name:"Home"});expect(screen.queryByLabelText("Widget type")).not.toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Open Editor"}));await waitFor(()=>expect(mockedExperiences.createExperience).toHaveBeenCalledWith("org_1","site_1",expect.objectContaining({widgetType:"modal",buildPageId:"pg_1",useBuildPageAsTarget:false})));expect(mockedExperiences.createEditorSession).not.toHaveBeenCalled();expect(mockedExperiences.createExperience.mock.calls[0][2]).not.toHaveProperty("definition");});
  it("cancel creates no record and a manual URL outside the site is rejected",async()=>{const onOpen=vi.fn();render(<MemoryRouter><CreateExperienceModal kind="guide" open onOpenChange={onOpen}/></MemoryRouter>);await screen.findByRole("option",{name:"Home"});fireEvent.change(screen.getByLabelText("Build on page"),{target:{value:"manual"}});fireEvent.change(screen.getByLabelText("Page URL"),{target:{value:"https://evil.example.com"}});fireEvent.click(screen.getByRole("button",{name:"Open Editor"}));expect(await screen.findByText(/configured domain/i)).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Cancel"}));expect(mockedExperiences.createExperience).not.toHaveBeenCalled();expect(onOpen).toHaveBeenCalledWith(false);});
  it("surfaces target-required publication validation from the draft/publish UI",async()=>{mockedExperiences.getExperience.mockResolvedValue(experience);mockedExperiences.updateExperience.mockResolvedValue(experience);mockedExperiences.publishExperience.mockRejectedValue(new Error("target_required"));render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);expect(await screen.findByText("Welcome")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Publish"}));expect(await screen.findByText(/every guide step/i)).toBeInTheDocument();});
  it("keeps the builder visible while compact editor options open drawers",async()=>{mockedExperiences.getExperience.mockResolvedValue(experience);render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);expect(await screen.findByLabelText("Builder experience key")).toBeInTheDocument();expect(screen.queryByRole("button",{name:"Builder"})).not.toBeInTheDocument();fireEvent.pointerDown(screen.getByRole("button",{name:"Editor options"}),{button:0,ctrlKey:false});fireEvent.click(await screen.findByRole("menuitem",{name:"Behavior"}));expect(screen.getByRole("dialog",{name:"Behavior"})).toBeInTheDocument();expect(screen.getByLabelText("Builder experience key")).toBeInTheDocument();expect(screen.getByRole("dialog",{name:"Behavior"})).toHaveAttribute("aria-modal","false");});
  it("flushes the latest builder edit before publishing",async()=>{mockedExperiences.getExperience.mockResolvedValue(experience);mockedExperiences.updateExperience.mockResolvedValue(experience);mockedExperiences.publishExperience.mockResolvedValue(experience);render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);fireEvent.click(await screen.findByRole("button",{name:"Change builder"}));fireEvent.click(screen.getByRole("button",{name:"Publish"}));await waitFor(()=>expect(mockedExperiences.publishExperience).toHaveBeenCalled());expect(mockedExperiences.updateExperience.mock.calls[0][3]).toMatchObject({definition:{builder:{version:1,html:expect.stringContaining("Latest heading")},content:{heading:"Latest heading"}}});expect(mockedExperiences.updateExperience.mock.invocationCallOrder[0]).toBeLessThan(mockedExperiences.publishExperience.mock.invocationCallOrder[0]);});
  it("flushes and saves the latest builder edit before creating a live-preview session",async()=>{mockedExperiences.getExperience.mockResolvedValue(experience);mockedExperiences.updateExperience.mockResolvedValue(experience);mockedExperiences.createEditorSession.mockResolvedValue({sessionId:"ees",launchUrl:"https://app.example.com/home?movecues_editor_token=token",expiresAt:"2026-01-01"});render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);fireEvent.click(await screen.findByRole("button",{name:"Change builder"}));fireEvent.click(screen.getByRole("button",{name:"Open live editor"}));await waitFor(()=>expect(mockedExperiences.createEditorSession).toHaveBeenCalled());expect(builderHarness.flush).toHaveBeenCalled();expect(mockedExperiences.updateExperience.mock.calls[0][3]).toMatchObject({definition:{builder:{html:expect.stringContaining("Latest heading")}}});expect(mockedExperiences.updateExperience.mock.invocationCallOrder[0]).toBeLessThan(mockedExperiences.createEditorSession.mock.invocationCallOrder[0]);expect(window.open).toHaveBeenCalledWith("about:blank","movecues-live-editor",expect.stringContaining("popup=yes"));expect(popupHarness.location.replace).toHaveBeenCalledWith("https://app.example.com/home?movecues_editor_token=token");expect(popupHarness.focus).toHaveBeenCalled();});
  it("keeps separate builder state for each Guide step",async()=>{const guide=guideExperience();mockedExperiences.getExperience.mockResolvedValue(guide);mockedExperiences.updateExperience.mockResolvedValue(guide);mockedExperiences.publishExperience.mockResolvedValue(guide);render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);expect(await screen.findByLabelText("Builder experience key")).toBeInTheDocument();expect(screen.getAllByRole("button",{name:"Change builder"})).toHaveLength(1);fireEvent.click(screen.getByRole("button",{name:"Change builder"}));fireEvent.click(screen.getByRole("button",{name:/2 Create project/}));expect(await screen.findByLabelText("Builder experience key")).toHaveTextContent(/:step_2$/);expect(screen.getByLabelText("Loaded builder HTML")).toHaveTextContent("No builder");fireEvent.click(screen.getByRole("button",{name:"Change builder"}));fireEvent.click(screen.getByRole("button",{name:/1 Welcome edited/}));expect(await screen.findByLabelText("Loaded builder HTML")).toHaveTextContent("Welcome edited");fireEvent.click(screen.getByRole("button",{name:"Publish"}));await waitFor(()=>expect(mockedExperiences.publishExperience).toHaveBeenCalled());const saved=mockedExperiences.updateExperience.mock.calls.at(-1)![3].definition!;expect("steps" in saved&&saved.steps[0].builder?.html).toContain("Welcome edited");expect("steps" in saved&&saved.steps[1].builder?.html).toContain("Create project edited");});
  it("flushes the active Guide builder before mounting the selected step",async()=>{const guide=guideExperience();mockedExperiences.getExperience.mockResolvedValue(guide);render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);await screen.findByLabelText("Builder experience key");builderHarness.flush.mockClear();builderHarness.render.mockClear();fireEvent.click(screen.getByRole("button",{name:/2 Create project/}));await waitFor(()=>expect(screen.getByLabelText("Builder experience key")).toHaveTextContent(/:step_2$/));expect(builderHarness.flush).toHaveBeenCalledWith(expect.stringMatching(/:step_1$/));expect(builderHarness.flush.mock.invocationCallOrder[0]).toBeLessThan(builderHarness.render.mock.invocationCallOrder.at(-1)!);expect(screen.getAllByRole("button",{name:"Change builder"})).toHaveLength(1);});
  it("chooses a Guide step pattern before creating and selecting the new step",async()=>{const guide=guideExperience();mockedExperiences.getExperience.mockResolvedValue(guide);mockedExperiences.updateExperience.mockResolvedValue(guide);render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);expect(await screen.findByLabelText("Builder widget type")).toHaveTextContent("anchored_card");expect(screen.getAllByRole("button",{name:/^\d /})).toHaveLength(2);builderHarness.flush.mockClear();fireEvent.click(screen.getByRole("button",{name:"Add step"}));expect(screen.getByRole("dialog",{name:"Add step"})).toBeInTheDocument();expect(screen.getAllByRole("button",{name:/^\d /})).toHaveLength(2);expect(builderHarness.flush).not.toHaveBeenCalled();fireEvent.click(screen.getByRole("button",{name:"Close Add step"}));expect(screen.queryByRole("dialog",{name:"Add step"})).not.toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Add step"}));fireEvent.click(screen.getByRole("button",{name:/^Modal/}));await waitFor(()=>expect(screen.getByLabelText("Builder widget type")).toHaveTextContent("modal"));expect(screen.getByRole("button",{name:/3 Next step · Modal/})).not.toHaveTextContent("⚠");expect(builderHarness.flush).toHaveBeenCalledWith(expect.stringMatching(/:step_1$/));fireEvent.pointerDown(screen.getByRole("button",{name:"Editor options"}),{button:0,ctrlKey:false});fireEvent.click(await screen.findByRole("menuitem",{name:"Step settings"}));expect(screen.queryByLabelText("Placement")).not.toBeInTheDocument();expect(screen.queryByText(/select this step's DOM target/i)).not.toBeInTheDocument();expect(screen.queryByText("User clicks target element")).not.toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Close Step settings"}));fireEvent.click(screen.getByRole("button",{name:"Add step"}));fireEvent.click(screen.getByRole("button",{name:/^Anchored card/}));await waitFor(()=>expect(screen.getByLabelText("Builder widget type")).toHaveTextContent("anchored_card"));expect(screen.getByRole("button",{name:/4 Next step · Anchored/})).toHaveTextContent("⚠");});
  it("uses the existing compact navigation in the editor and restores normal navigation on exit",async()=>{const guide=guideExperience();mockedExperiences.getExperience.mockResolvedValue(guide);render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route element={<AppShell/>}><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/><Route path="/experiences/guides" element={<div>Guide collection</div>}/></Route></Routes></MemoryRouter>);expect(await screen.findByRole("button",{name:"Expand navigation"})).toBeInTheDocument();expect(screen.getByRole("link",{name:"Guides"})).toHaveAttribute("href","/experiences/guides");fireEvent.click(screen.getByRole("button",{name:"Expand navigation"}));expect(screen.getByRole("button",{name:"Collapse navigation"})).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Collapse navigation"}));expect(screen.getByRole("button",{name:"Expand navigation"})).toBeInTheDocument();fireEvent.click(screen.getByRole("link",{name:"Back to Experiences"}));expect(await screen.findByText("Guide collection")).toBeInTheDocument();expect(screen.getByRole("button",{name:"Collapse navigation"})).toBeInTheDocument();});
  it("keeps the selected Guide step stable when moving and deleting from its contextual menu",async()=>{const guide=guideExperience();mockedExperiences.getExperience.mockResolvedValue(guide);render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);fireEvent.click(await screen.findByRole("button",{name:/2 Create project/}));builderHarness.flush.mockClear();fireEvent.pointerDown(screen.getByRole("button",{name:"Actions for step 2"}),{button:0,ctrlKey:false});fireEvent.click(await screen.findByRole("menuitem",{name:"Move up"}));await waitFor(()=>expect(screen.getByRole("button",{name:/1 Create project/})).toBeInTheDocument());expect(screen.getByLabelText("Builder experience key")).toHaveTextContent(/:step_2$/);expect(builderHarness.flush).toHaveBeenCalled();fireEvent.pointerDown(screen.getByRole("button",{name:"Actions for step 1"}),{button:0,ctrlKey:false});fireEvent.click(await screen.findByRole("menuitem",{name:"Delete"}));await waitFor(()=>expect(screen.getByLabelText("Builder experience key")).toHaveTextContent(/:step_1$/));expect(screen.queryByRole("button",{name:/Create project/})).not.toBeInTheDocument();});
  it("opens Guide step 1 on its saved page while preserving editor session parameters",async()=>{const guide=guideExperience();mockedExperiences.getExperience.mockResolvedValue(guide);mockedExperiences.updateExperience.mockResolvedValue(guide);mockedExperiences.createEditorSession.mockResolvedValue({sessionId:"ees",launchUrl:"https://app.example.com/home?movecues_editor_token=token&movecues_editor_session=session",expiresAt:"2026-01-01"});render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);await screen.findByLabelText("Builder experience key");fireEvent.click(screen.getByRole("button",{name:"Open live editor"}));await waitFor(()=>expect(popupHarness.location.replace).toHaveBeenCalledWith("https://app.example.com/onboarding?source=guide&movecues_editor_token=token&movecues_editor_session=session&movecues_editor_step=0#intro"));});
  it("opens the selected Guide step on that step's saved page",async()=>{const guide=guideExperience();mockedExperiences.getExperience.mockResolvedValue(guide);mockedExperiences.updateExperience.mockResolvedValue(guide);mockedExperiences.createEditorSession.mockResolvedValue({sessionId:"ees",launchUrl:"https://app.example.com/home?movecues_editor_token=token",expiresAt:"2026-01-01"});render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);await screen.findByLabelText("Builder experience key");fireEvent.click(screen.getByRole("button",{name:/2 Create project/}));fireEvent.click(screen.getByRole("button",{name:"Open live editor"}));await waitFor(()=>expect(popupHarness.location.replace).toHaveBeenCalledWith("https://app.example.com/projects/new?template=blank&movecues_editor_token=token&movecues_editor_step=1#details"));});
  it("falls back to the session launch URL when a Guide step has no saved page",async()=>{const guide=guideExperience();const guideDefinition=guide.draftVersion!.definition;if(!("steps" in guideDefinition)) throw new Error("Expected a Guide definition");delete guideDefinition.steps[1].target!.targetContext;mockedExperiences.getExperience.mockResolvedValue(guide);mockedExperiences.updateExperience.mockResolvedValue(guide);mockedExperiences.createEditorSession.mockResolvedValue({sessionId:"ees",launchUrl:"https://app.example.com/home?movecues_editor_token=token",expiresAt:"2026-01-01"});render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);await screen.findByLabelText("Builder experience key");fireEvent.click(screen.getByRole("button",{name:/2 Create project/}));fireEvent.click(screen.getByRole("button",{name:"Open live editor"}));await waitFor(()=>expect(popupHarness.location.replace).toHaveBeenCalledWith("https://app.example.com/home?movecues_editor_token=token&movecues_editor_step=1"));});
  it("flushes and preserves independent survey slide builders when switching slides",async()=>{const survey=surveyExperience();mockedExperiences.getExperience.mockResolvedValue(survey);mockedExperiences.updateExperience.mockResolvedValue(survey);render(<MemoryRouter initialEntries={["/experiences/exp_1/edit"]}><Routes><Route path="/experiences/:experienceId/edit" element={<ExperienceEditorPage/>}/></Routes></MemoryRouter>);expect(await screen.findByRole("complementary",{name:"Survey step rail"})).toBeInTheDocument();expect(screen.getByLabelText("Builder experience key")).toHaveTextContent(/:survey_1$/);fireEvent.click(screen.getByRole("button",{name:"Change builder"}));const slideOneProject=screen.getByLabelText("Loaded builder project").textContent;expect(slideOneProject).toContain("survey_1");expect(slideOneProject).toContain('"left":140');builderHarness.flush.mockClear();builderHarness.render.mockClear();fireEvent.click(screen.getByRole("button",{name:/2 Follow-up/}));await waitFor(()=>expect(screen.getByLabelText("Builder experience key")).toHaveTextContent(/:survey_2$/));expect(builderHarness.flush).toHaveBeenCalledWith(expect.stringMatching(/:survey_1$/));expect(builderHarness.flush.mock.invocationCallOrder[0]).toBeLessThan(builderHarness.render.mock.invocationCallOrder.at(-1)!);fireEvent.click(screen.getByRole("button",{name:"Change builder"}));expect(screen.getByLabelText("Loaded builder project")).toHaveTextContent("survey_2");fireEvent.click(screen.getByRole("button",{name:/1 Intro edited/}));expect(await screen.findByLabelText("Loaded builder HTML")).toHaveTextContent("Intro edited");expect(screen.getByLabelText("Loaded builder project").textContent).toBe(slideOneProject);});
});
